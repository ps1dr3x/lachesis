services:
  db:
    image: postgres:17
    restart: always
    environment:
      POSTGRES_PASSWORD: insecure
      POSTGRES_USER: lachesis_agent
      POSTGRES_DB: lachesis_dev
    ports:
      - 5432:5432

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080